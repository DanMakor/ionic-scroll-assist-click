import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'readonly-scroll-assist',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
